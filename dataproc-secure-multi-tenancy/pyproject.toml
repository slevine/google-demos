[project]
name = "dataproc-secure-multi-tenancy-demo"
version = "0.1.0"
description = "A PySpark project to demonstrate service account based Secure Multi-tenancy."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=3.5.6"
]
