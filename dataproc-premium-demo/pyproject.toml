[project]
name = "serverless-spark-premium-demo"
version = "0.1.0"
description = "A PySpark project to benchmark Dataproc Serverless performance."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=3.5.6"
]
